[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qsc-solo-04"
version = "0.1.0"
description = "Mission and observation quality platform for autonomous ocean surveys"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
surveyops = "qsc_solo.api:main"

[tool.setuptools.packages.find]
include = ["qsc_solo*"]
